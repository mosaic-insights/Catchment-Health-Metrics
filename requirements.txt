# === Array / SciPy stack ===
numpy>=1.24
pandas>=2.0
scipy>=1.11
matplotlib>=3.8
tqdm>=4.66

# === Geospatial core ===
xarray>=2023.12.0
rasterio>=1.3
rioxarray>=0.15
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
affine>=2.4

# === STAC / remote sensing ===
pystac-client>=0.7
odc-stac>=0.3

# === Hydro tools ===
pysheds>=0.3

# === IO / utilities ===
requests>=2.31

# === CLI / config ===
typer>=0.12.3
pydantic>=2.5
pyyaml>=6.0
rasterstats>=0.19,
# === DEA tools (optional; heavy) ===
# dea-tools>=0.3
# NOTE: dea-tools may require system deps (e.g., GDAL stack).